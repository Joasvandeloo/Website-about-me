# Check what's changed
git status

# Stage your changes
git add .                # everything

# Commit with a message
git commit -m "Add login form validation"

# Push to GitHub
git push origin main
